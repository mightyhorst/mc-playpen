import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface DeleteVideoProps{
    children?: ReactNode;
}
export function DeleteVideo({

}:DeleteVideoProps){
    return (
        <>
            
        </>
    );
}
export default memo(DeleteVideo);
