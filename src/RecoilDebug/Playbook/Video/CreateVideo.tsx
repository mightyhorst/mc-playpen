import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface CreateVideoProps{
    children?: ReactNode;
}
export function CreateVideo({

}:CreateVideoProps){
    return (
        <form>
            
        </form>
    );
}
export default memo(CreateVideo);
