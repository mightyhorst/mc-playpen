import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface UpdateVideoProps{
    children?: ReactNode;
}
export function UpdateVideo({

}:UpdateVideoProps){
    return (
        <>
            
        </>
    );
}
export default memo(UpdateVideo);
