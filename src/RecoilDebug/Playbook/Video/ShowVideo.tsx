import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface ShowVideoProps{
    children?: ReactNode;
}
export function ShowVideo({

}:ShowVideoProps){
    return (
        <>
            
        </>
    );
}
export default memo(ShowVideo);
