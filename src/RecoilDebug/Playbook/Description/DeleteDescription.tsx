import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface DeleteDescriptionProps{
    children?: ReactNode;
}
export function DeleteDescription({

}:DeleteDescriptionProps){
    return (
        <>
            
        </>
    );
}
export default memo(DeleteDescription);
