import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface UpdateDescriptionProps{
    children?: ReactNode;
}
export function UpdateDescription({

}:UpdateDescriptionProps){
    return (
        <>
            
        </>
    );
}
export default memo(UpdateDescription);
