import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface ListDescriptionProps{
    children?: ReactNode;
}
export function ListDescription({

}:ListDescriptionProps){
    return (
        <>
            
        </>
    );
}
export default memo(ListDescription);
