import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface UpdateCodeProps{
    children?: ReactNode;
}
export function UpdateCode({

}:UpdateCodeProps){
    return (
        <>
            
        </>
    );
}
export default memo(UpdateCode);
