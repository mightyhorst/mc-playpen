import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface ShowCodeProps{
    children?: ReactNode;
}
export function ShowCode({

}:ShowCodeProps){
    return (
        <>
            
        </>
    );
}
export default memo(ShowCode);
