import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface DeleteCodeProps{
    children?: ReactNode;
}
export function DeleteCode({

}:DeleteCodeProps){
    return (
        <>
            
        </>
    );
}
export default memo(DeleteCode);
