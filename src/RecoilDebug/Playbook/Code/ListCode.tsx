import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface ListCodeProps{
    children?: ReactNode;
}
export function ListCode({

}:ListCodeProps){
    return (
        <>
            
        </>
    );
}
export default memo(ListCode);
