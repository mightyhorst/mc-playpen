import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface CreateCodeProps{
    children?: ReactNode;
}
export function CreateCode({

}:CreateCodeProps){
    return (
        <>
            
        </>
    );
}
export default memo(CreateCode);
