import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface ShowAudioProps{
    children?: ReactNode;
}
export function ShowAudio({

}:ShowAudioProps){
    return (
        <>
            
        </>
    );
}
export default memo(ShowAudio);
