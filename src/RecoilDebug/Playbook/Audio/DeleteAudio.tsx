import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface DeleteAudioProps{
    children?: ReactNode;
}
export function DeleteAudio({

}:DeleteAudioProps){
    return (
        <>
            
        </>
    );
}
export default memo(DeleteAudio);
