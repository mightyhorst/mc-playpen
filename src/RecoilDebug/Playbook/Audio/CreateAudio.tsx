import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface CreateAudioProps{
    children?: ReactNode;
}
export function CreateAudio({

}:CreateAudioProps){
    return (
        <>
            
        </>
    );
}
export default memo(CreateAudio);
