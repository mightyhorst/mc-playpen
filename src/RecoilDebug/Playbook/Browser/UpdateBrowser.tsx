import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface UpdateBrowserProps{
    children?: ReactNode;
}
export function UpdateBrowser({

}:UpdateBrowserProps){
    return (
        <>
            
        </>
    );
}
export default memo(UpdateBrowser);
