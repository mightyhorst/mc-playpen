import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface ShowBrowserProps{
    children?: ReactNode;
}
export function ShowBrowser({

}:ShowBrowserProps){
    return (
        <>
            
        </>
    );
}
export default memo(ShowBrowser);
