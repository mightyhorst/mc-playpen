import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface DeleteBrowserProps{
    children?: ReactNode;
}
export function DeleteBrowser({

}:DeleteBrowserProps){
    return (
        <>
            
        </>
    );
}
export default memo(DeleteBrowser);
