import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface CreateSpreadsheetProps{
    children?: ReactNode;
}
export function CreateSpreadsheet({

}:CreateSpreadsheetProps){
    return (
        <form>
            
        </form>
    );
}
export default memo(CreateSpreadsheet);
