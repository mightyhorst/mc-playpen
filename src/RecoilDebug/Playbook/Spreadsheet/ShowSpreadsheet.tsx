import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface ShowSpreadsheetProps{
    children?: ReactNode;
}
export function ShowSpreadsheet({

}:ShowSpreadsheetProps){
    return (
        <>
            
        </>
    );
}
export default memo(ShowSpreadsheet);
