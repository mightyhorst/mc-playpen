import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface DeleteSpreadsheetProps{
    children?: ReactNode;
}
export function DeleteSpreadsheet({

}:DeleteSpreadsheetProps){
    return (
        <>
            
        </>
    );
}
export default memo(DeleteSpreadsheet);
