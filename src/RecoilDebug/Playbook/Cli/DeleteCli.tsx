import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface DeleteCliProps{
    children?: ReactNode;
}
export function DeleteCli({

}:DeleteCliProps){
    return (
        <>
            
        </>
    );
}
export default memo(DeleteCli);
