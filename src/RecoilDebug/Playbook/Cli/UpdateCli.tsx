import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface UpdateCliProps{
    children?: ReactNode;
}
export function UpdateCli({

}:UpdateCliProps){
    return (
        <>
            
        </>
    );
}
export default memo(UpdateCli);
