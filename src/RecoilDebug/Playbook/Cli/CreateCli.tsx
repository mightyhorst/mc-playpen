import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface CreateCliProps{
    children?: ReactNode;
}
export function CreateCli({

}:CreateCliProps){
    return (
        <form>
            
        </form>
    );
}
export default memo(CreateCli);
