import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface ShowCliProps{
    children?: ReactNode;
}
export function ShowCli({

}:ShowCliProps){
    return (
        <>
            
        </>
    );
}
export default memo(ShowCli);
