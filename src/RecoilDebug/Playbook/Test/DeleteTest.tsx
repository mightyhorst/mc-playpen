import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface DeleteTestProps{
    children?: ReactNode;
}
export function DeleteTest({

}:DeleteTestProps){
    return (
        <>
            
        </>
    );
}
export default memo(DeleteTest);
