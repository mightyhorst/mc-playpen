import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface UpdateTestProps{
    children?: ReactNode;
}
export function UpdateTest({

}:UpdateTestProps){
    return (
        <>
            
        </>
    );
}
export default memo(UpdateTest);
