import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface DeleteTimelineProps{
    children?: ReactNode;
}
export function DeleteTimeline({

}:DeleteTimelineProps){
    return (
        <>
            
        </>
    );
}
export default memo(DeleteTimeline);
