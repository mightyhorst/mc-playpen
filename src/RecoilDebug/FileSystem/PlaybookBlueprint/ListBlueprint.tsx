import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface ListBlueprintProps{
    children?: ReactNode;
}
export function ListBlueprint({

}:ListBlueprintProps){
    return (
        <form>
            
        </form>
    );
}
export default memo(ListBlueprint);
