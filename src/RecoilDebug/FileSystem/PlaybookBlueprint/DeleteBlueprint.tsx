import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface DeleteBlueprintProps{
    children?: ReactNode;
}
export function DeleteBlueprint({

}:DeleteBlueprintProps){
    return (
        <>
            
        </>
    );
}
export default memo(DeleteBlueprint);
