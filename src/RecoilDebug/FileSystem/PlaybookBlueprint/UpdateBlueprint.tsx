import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface UpdateBlueprintProps{
    children?: ReactNode;
}
export function UpdateBlueprint({

}:UpdateBlueprintProps){
    return (
        <>
            
        </>
    );
}
export default memo(UpdateBlueprint);
