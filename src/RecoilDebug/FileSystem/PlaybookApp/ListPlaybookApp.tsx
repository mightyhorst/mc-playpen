import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface ListPlaybookAppProps{
    children?: ReactNode;
}
export function ListPlaybookApp({

}:ListPlaybookAppProps){
    return (
        <>
            
        </>
    );
}
export default memo(ListPlaybookApp);
