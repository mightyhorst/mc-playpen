import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface ShowPlaybookAppProps{
    children?: ReactNode;
}
export function ShowPlaybookApp({

}:ShowPlaybookAppProps){
    return (
        <>
            
        </>
    );
}
export default memo(ShowPlaybookApp);
