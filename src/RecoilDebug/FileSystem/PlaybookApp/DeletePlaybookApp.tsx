import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface DeletePlaybookAppProps{
    children?: ReactNode;
}
export function DeletePlaybookApp({

}:DeletePlaybookAppProps){
    return (
        <>
            
        </>
    );
}
export default memo(DeletePlaybookApp);
