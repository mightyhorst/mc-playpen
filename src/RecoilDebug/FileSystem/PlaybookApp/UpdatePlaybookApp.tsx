import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface UpdatePlaybookAppProps{
    children?: ReactNode;
}
export function UpdatePlaybookApp({

}:UpdatePlaybookAppProps){
    return (
        <>
            
        </>
    );
}
export default memo(UpdatePlaybookApp);
