import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface CreatePlaybookAppProps{
    children?: ReactNode;
}
export function CreatePlaybookApp({

}:CreatePlaybookAppProps){
    return (
        <form>
            
        </form>
    );
}
export default memo(CreatePlaybookApp);
