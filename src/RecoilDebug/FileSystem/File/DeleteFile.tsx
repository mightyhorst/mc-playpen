import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface DeleteFileProps{
    children?: ReactNode;
}
export function DeleteFile({

}:DeleteFileProps){
    return (
        <>
            
        </>
    );
}
export default memo(DeleteFile);
