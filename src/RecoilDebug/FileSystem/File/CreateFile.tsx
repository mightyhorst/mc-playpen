import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface CreateFileProps{
    children?: ReactNode;
}
export function CreateFile({

}:CreateFileProps){
    return (
        <form>
            
        </form>
    );
}
export default memo(CreateFile);
