import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface ShowFileProps{
    children?: ReactNode;
}
export function ShowFile({

}:ShowFileProps){
    return (
        <>
            
        </>
    );
}
export default memo(ShowFile);
