import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface UpdateFileProps{
    children?: ReactNode;
}
export function UpdateFile({

}:UpdateFileProps){
    return (
        <>
            
        </>
    );
}
export default memo(UpdateFile);
