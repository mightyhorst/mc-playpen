import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface UpdateFolderProps{
    children?: ReactNode;
}
export function UpdateFolder({

}:UpdateFolderProps){
    return (
        <>
            
        </>
    );
}
export default memo(UpdateFolder);
