import { 
    memo,
    ReactNode, 
} from 'react';
import {

} from '../../../recoil';
import {

} from '../../../models';
import {

} from '../../../components';
import {

} from '../../../hooks';
interface DeleteFolderProps{
    children?: ReactNode;
}
export function DeleteFolder({

}:DeleteFolderProps){
    return (
        <>
            
        </>
    );
}
export default memo(DeleteFolder);
