import {
    useState,
    useEffect,
} from 'react';

/**
 * @requires Models
 */
import {
    IMaster,
    IPartial,
} from '../models';

export function usePrinter({
    master,
    partials,
}:{
    master: IMaster;
    partials: IPartial[];
}){

}
export default usePrinter;
